# Melihat docker image
docker image ls

# Mendownload docker image
docker image pull namaimage:tag #format || list image cek di https://hub.docker.com/
docker image pull nginx:latest #implementasi

# Menghapus docker image
docker image rm namaimage:tag #format
docker image rm nginx:latest #implementasi

