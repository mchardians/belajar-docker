# Melihat volume
docker volume ls

# Membuat volume
docker volume create namavolume #format
docker volume create mysqlvolume #implementasi

# Menghapus volume
docker volume rm namavolume #format
docker volume rm mysqlvolume #implementasi